# Environment configuration for mcp_server/config.py

# Base URL for the API
api_base_url=https://api.example.com

# Path to the token file (default is /tmp/token.json)
token_path=/tmp/token.json

# Credentials for Kanban
kanban_username=your_kanban_username
kanban_password=your_kanban_password
