# Environment configuration for mcp_server/config.py

# Base URL for the API
MCP_API_BASE_URL=https://api.example.com

# Path to the token file (default is /tmp/token.json)
MCP_TOKEN_PATH=/tmp/token.json

# Credentials for Kanban
MCP_KANBAN_USERNAME=your_kanban_username
MCP_KANBAN_PASSWORD=your_kanban_password
